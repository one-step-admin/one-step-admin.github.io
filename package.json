{
  "name": "fantastic-admin-docs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docs:dev": "vuepress dev docs-source",
    "docs:build": "vuepress build docs-source",
    "deploy": "git checkout --orphan temporary_branch && git add -A && git commit -am \"deploy\" && git branch -D docs && git branch -m docs && git push -f origin docs && git branch --set-upstream-to=origin/docs docs"
  },
  "author": "hooray",
  "license": "MIT",
  "devDependencies": {
    "@vuepress/plugin-docsearch": "^2.0.0-beta.26",
    "vuepress": "^2.0.0-beta.26",
    "vuepress-vite": "^2.0.0-beta.26"
  }
}
